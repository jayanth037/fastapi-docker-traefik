apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.fastapi.service.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.fastapi.deployment.app }}
  ports:
    - protocol: TCP
      port: {{ .Values.fastapi.service.port }}
      targetPort: {{ .Values.fastapi.service.targetPort }}
